{
  "name": "together",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "dev:https": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev:https -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server",
    "typecheck": "npm run typecheck -w server && npm run typecheck -w client",
    "assets:characters": "node scripts/build-character-assets.mjs",
    "atlas": "node scripts/build-atlas.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "pngjs": "^7.0.0"
  }
}
